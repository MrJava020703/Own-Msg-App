import {useState}from'react';export function JoinScreen({onJoin}:{onJoin:(name:string)=>Promise<void>}){const[n,setN]=useState('');const[e,setE]=useState('');return <section className="join"><form onSubmit={async x=>{x.preventDefault();try{await onJoin(n)}catch{setE('Could not join. Check the server connection.')}}}><h1>REALTIME CHAT</h1><label>Enter your name<input autoFocus value={n} onChange={x=>setN(x.target.value)} maxLength={80} required placeholder="Saurabh Kumar"/></label>{e&&<p role="alert">{e}</p>}<button>JOIN CHAT</button></form></section>}
