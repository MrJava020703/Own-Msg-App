import{useEffect,useRef,useState}from'react';import type{Conversation,Message,User}from'../../types';import{socket}from'../../services/socket';import{MessageBubble}from'./MessageBubble';const emojis=['😀','😁','😂','🤣','😊','😍','😘','😎','🤔','😭','👍','👏','🙏','🔥','❤️','🐶','🍔','⚽','🚗','💡','🇮🇳'];export function ChatPanel({me,conversation,messages,typing,onMessage}:{me:User;conversation:Conversation|null;messages:Message[];typing:string|null;onMessage:(m:Message)=>void}){const[text,setText]=useState(''),[picker,setPicker]=useState(false),[timer,setTimer]=useState<number|undefined>();const bottom=useRef<HTMLDivElement>(null),pick=useRef<HTMLDivElement>(null);const peer=conversation?.participants.map(p=>p.user).find(u=>u.id!==me.id);useEffect(()=>bottom.current?.scrollIntoView({behavior:'smooth'}),[messages]);useEffect(()=>{const close=(e:MouseEvent)=>{if(!pick.current?.contains(e.target as Node))setPicker(false)};document.addEventListener('mousedown',close);return()=>document.removeEventListener('mousedown',close)},[]);if(!conversation||!peer)return <section className="empty">Select a conversation<br/><small>Choose someone to start chatting</small></section>;const send=()=>{if(!text.trim())return;socket.emit('message:send',{conversationId:conversation.id,receiverId:peer.id,type:'TEXT',content:text},(r:any)=>r.success&&onMessage(r.data));setText('');socket.emit('typing:stop',{conversationId:conversation.id,receiverId:peer.id})};return <section className="chat"><header><div><b>{peer.displayName}</b><small>{peer.status==='ONLINE'?'Online':peer.lastSeen?`Last seen ${new Date(peer.lastSeen).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})}`:'Offline'}</small></div><button type="button" aria-label="Voice call" title="Voice call" onClick={()=>window.dispatchEvent(new CustomEvent('call',{detail:{peer,type:'VOICE'}}))}>📞</button><button type="button" aria-label="Video call" title="Video call" onClick={()=>window.dispatchEvent(new CustomEvent('call',{detail:{peer,type:'VIDEO'}}))}>📹</button></header><div className="messages">{messages.map(m=><MessageBubble key={m.id} message={m} me={me}/>)}{typing&&<p className="typing">{typing} is typing…</p>}<div ref={bottom}/></div><form className="composer" onSubmit={e=>{e.preventDefault();send()}}><div className="emoji-wrap" ref={pick}><button type="button" aria-label="Open emoji picker" title="Open emoji picker" onClick={()=>setPicker(!picker)}>😀</button>{picker&&<div className="emoji-picker" role="dialog" aria-label="Emoji picker"><b>Smileys · People · Nature · Food · Activities · Travel · Objects · Symbols · Flags</b><div>{emojis.map(e=><button type="button" key={e} onClick={()=>{setText(t=>t+e);setPicker(false)}}>{e}</button>)}</div></div>}</div><input value={text} onChange={e=>{setText(e.target.value);socket.emit('typing:start',{conversationId:conversation.id,receiverId:peer.id});window.clearTimeout(timer);setTimer(window.setTimeout(()=>socket.emit('typing:stop',{conversationId:conversation.id,receiverId:peer.id}),900))}} placeholder="Type a message..."/><button aria-label="Send message">➤</button></form></section>}
