import{useState}from'react';import type{Message,User}from'../../types';import{socket}from'../../services/socket';const emojis=['❤️','😂','👍','🔥','😮','😢','👏','🎉','🙏'];export function MessageBubble({message:m,me}:{message:Message;me:User}){const[open,setOpen]=useState(false),mine=m.senderId===me.id;const reactions=Array.isArray(m.reactions)?m.reactions:[];const counts=Object.entries(reactions.reduce<Record<string,number>>((a,r)=>(a[r.emoji]=(a[r.emoji]??0)+1,a),{}));const ticks=m.delivery==='READ'?'✓✓':m.delivery==='DELIVERED'?'✓✓':'✓';return <article className={'bubble '+(mine?'mine':'')}><div>{m.deletedAt?'This message was deleted':m.content??''}</div><footer>{counts.map(([e,n])=><span key={e}>{e} {n}</span>)}{!m.deletedAt&&<button aria-label="Add reaction" onClick={()=>setOpen(!open)}>♡</button>}{open&&<span className="reaction-picker">{emojis.map(e=><button key={e} onClick={()=>{socket.emit('message:react',{messageId:m.id,emoji:e},()=>{});setOpen(false)}}>{e}</button>)}</span>}<time>{m.createdAt?new Date(m.createdAt).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}):''}{mine&&<b className={m.delivery==='READ'?'read':''}> {ticks}</b>}</time></footer></article>}
